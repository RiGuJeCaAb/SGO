# Testes

Antes de cada entrega:

1. Validar a sintaxe do `<script>` da aplicação isolado.
2. Correr um teste funcional em jsdom que exercite o caminho alterado.

Os testes mantêm-se entre sessões. Ainda não há arnês montado — depende de a aplicação
estar em `app/`.
